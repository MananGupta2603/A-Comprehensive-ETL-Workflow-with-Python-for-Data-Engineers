
import logging

logging.basicConfig(filename='A-Comprehensive-ETL-Workflow-with-Python-for-Data-Engineers/logs/log_file.txt', 
                    level=logging.INFO, 
                    format='%(asctime)s - %(levelname)s - %(message)s')

logging.info('ETL process started')
